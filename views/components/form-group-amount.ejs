<label for="<%= forName %>"
  ><%- label %> <%= units ? `(in ${units})` : '' %></label
>
<input
  type="number"
  id="<%= forName %>"
  name="<%= forName %>"
  class="form-control"
  min="0"
  value="<%= value ? value : '' %>"
/>
