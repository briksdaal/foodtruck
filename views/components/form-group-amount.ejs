<label for="<%= forName %>"
  ><%= label %> <%= units ? `(in ${units})` : '' %></label
>
<input
  type="number"
  id="amount"
  name="amount"
  class="form-control"
  min="0"
  value="<%= value ? value : '' %>"
/>
